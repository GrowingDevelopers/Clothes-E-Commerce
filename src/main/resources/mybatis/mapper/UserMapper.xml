<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="clothes.ecommerce.domain.user.UserRepository">
    <select id="findByUserNumber" resultType="User" parameterType="Long">
        select * from users where id = #{userNumber}
    </select>

    <insert id="save" useGeneratedKeys="true" keyProperty="userNumber">
        insert into users(userNumber, userId, userPassword, userName, userAddress, bankAccount, bankAccountAmount)
        values(#{userNumber}, #{userId}, #{userPassword}, #{userName}, #{userAddress}, #{bankAccount}, #{bankAccountAmount})
    </insert>
</mapper>